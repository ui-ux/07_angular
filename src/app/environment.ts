export const environment = {
  production: false,
  // true — production, false — development
  apiUrl: 'https://api.example.com',
  // The base URL for the production API
  enableStoreDevTools: false,
  // Disables DevTools in production
};
